node_modules/
.claude/
acronym-generator/
*.local.json
.DS_Store

# Build artifacts — rendered from template/index.html.template + data/*.json
# by scripts/render.js (CI) or served in-memory by scripts/dev.js (dev).
# Glob also catches iCloud Drive conflict copies ("index 2.html", …).
/index*.html
/_site/

# Sync TTL/hash cache metadata — local only
/data/.cache/
